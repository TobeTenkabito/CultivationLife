# Step 10 正式切换审计

审计日期：2026-09-19

## 结论

V2领域内核、存档框架、HTTP边界和正式客户端玩家可达面现已全部通过发布面审计。本轮仍**不执行V1退役、默认启动器切换或正式EXE覆盖**：按既定顺序，下一步还要完成全仓回归、候选EXE构建和全新目录验收后才能覆盖正式产物。

## 验证结果

- 本轮完整前端完成后的全仓回归：`677 passed`（`238.05s`）。
- V2源码HTTP冒烟：配置返回104项操作且冻结入口遗漏为0；主页、新建角色、耗时行动、Debug设置、领域错误翻译、存档读取与旧档发现接口均成功。
- 冻结V1公开操作：101项。
- V2 HTTP注册操作：104项；101个冻结入口已全部映射，另有统一`ascend-world`、显式`fight`和主动`market-refresh`三个V2入口。HTTP缺口为0。
- V2正式页面可触达操作：104项；101个冻结入口全部覆盖，另含`ascend-world`、`fight`和`market-refresh`三个V2原生入口。客户端缺口为0。
- 正式页面按修行、天地、因果、势力、营生、百艺、征伐、异道八组提供类型化表单；炼丹、炼器、九宫阵、飞升邀请、招募筛选和祖血规则均有专用结构化控件，不暴露原始JSON输入。
- V2 Playwright烟雾验证建角、八分区102张系统卡、配置候选项、复杂表单、真实HTTP提交和自动存档刷新，未发现页面脚本异常。
- V1/V2综合审计：101项冻结操作匹配、53/53功能通过、0结构错误、0阻断、0影子执行错误、0影子差异、发布面两端均为0缺口。
- 当前`launcher.py`继续指向V1，只等待全仓与候选EXE验收，不再存在发布面缺口。

完整机器可读结果见`latest_release_surface.json`。运行：

```powershell
python tools/check_v2_release_surface.py --report docs/v2/latest_release_surface.json
```

当前退出码为`0`且`ready: true`；退出码`2`表示未来再次出现缺口时禁止切换。

## 本轮已修复的发布问题

- V2冻结运行时不再把SQLite数据库放入PyInstaller临时解包目录；开发版`dist`仍复用项目持久化目录。
- 基础内容和网页从冻结资源目录读取，DLC/MOD从EXE同目录发现，二者不再错误绑定到同一路径。
- V2页面新增V1存档发现和单向导入入口；导入器仍先生成内容寻址备份，且不修改源存档。
- V2页面对存档名、事件和扩展内容进行HTML转义，避免外部内容注入页面结构。
- 新增正式发布面审计，防止以后再次出现“后端测试全绿、玩家却无法操作”的假阳性。
- 补齐99项缺失的正式交互入口，并为动作枚举逐项对照领域处理器，避免“按钮存在但后端拒绝动作值”的假覆盖。
- 生产投影公开可种植灵植与可炼制丹药目录，让客户端不依赖硬编码内容ID。

## 继续切换的硬门槛

1. ~~为剩余81项冻结操作建立V2 HTTP命令映射及端到端测试。~~ 已完成；注册表、参数绑定和应用方法签名均已逐项验证。
2. ~~为剩余99项冻结操作提供正式、非调试式的界面交互，并用浏览器冒烟覆盖。~~ 已完成；客户端缺口为0。
3. ~~发布面审计返回`ready: true`。~~ 已完成；仍须先通过全仓和候选包验收，才允许修改`launcher.py`和PyInstaller入口。
4. 构建候选EXE后，在全新目录验证新建、行动、保存、重启读取、V1导入、DLC发现，再覆盖正式产物。
